#!/usr/bin/env python

import sys
sys.path.insert(1, '@pythondir@')

from lucidgtk.dream import DreamWindow

if __name__ == "__main__":
    dw = DreamWindow(package="@PACKAGE@", version="@VERSION@")
    dw.run()
